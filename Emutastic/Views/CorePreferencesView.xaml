<UserControl x:Class="Emutastic.Views.CorePreferencesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="#2D2D2D" 
                Padding="20,15"
                CornerRadius="8,8,0,0">
            <StackPanel>
                <TextBlock Text="Core Preferences" 
                          FontSize="20" 
                          FontWeight="SemiBold" 
                          Foreground="White"/>
                <TextBlock Text="Select your preferred core for each console. The default core will be used if your preferred core is not available." 
                          FontSize="12" 
                          Foreground="#AAAAAA"
                          Margin="0,5,0,0"
                          TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Console List -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto"
                      Background="#1E1E1E">
            <ItemsControl x:Name="ConsoleList" Margin="10">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#2D2D2D" 
                                CornerRadius="6" 
                                Margin="0,5"
                                Padding="15,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Console Name -->
                                <TextBlock Grid.Column="0"
                                          Text="{Binding ConsoleName}"
                                          FontSize="14"
                                          FontWeight="SemiBold"
                                          Foreground="White"
                                          VerticalAlignment="Center"/>

                                <!-- Core Selection -->
                                <ComboBox Grid.Column="1"
                                         x:Name="CoreComboBox"
                                         ItemsSource="{Binding AvailableCores}"
                                         SelectedItem="{Binding SelectedCore, Mode=TwoWay}"
                                         DisplayMemberPath="DisplayName"
                                         Background="#3D3D3D"
                                         Foreground="White"
                                         BorderBrush="#555555"
                                         Padding="8,5"
                                         VerticalAlignment="Center">
                                    <ComboBox.Resources>
                                        <Style TargetType="ComboBoxItem">
                                            <Setter Property="Background" Value="#3D3D3D"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Style>
                                    </ComboBox.Resources>
                                </ComboBox>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
