# Build output
bin/
obj/

# Release artifacts
Emutastic-v*.zip

# Claude tooling
.claude/

# Secrets — OAuth client IDs, API keys (not committed; see Secrets.cs.template)
Emutastic/Secrets.cs

# Visual Studio
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

# Cores — large binary DLLs, not source code
Emutastic/Cores/*.dll
Emutastic/Cores/*.rom
Emutastic/Cores/dolphin-emu/

# BIOS files — must never be committed (copyrighted firmware)
Emutastic/BIOS/
Emutastic/Bios/
**/*.bin
**/bios*.rom
**/scph*.bin
**/saturn*.bin
**/mpr-*.bin
**/dc_boot.bin
**/dc_flash.bin

# DAT files — downloaded in-app via Preferences → Cores / Extras
Emutastic/DATs/*.dat

# Large database asset
Emutastic/Assets/openvgdb.sqlite

# Logs
*.log

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Local-only planning / design notes — never pushed to the public repo
/local-notes/

# Local dev tooling (release download stats, etc.)
build-stage/
